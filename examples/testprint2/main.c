#include <stdio.h>

int main(void) {
    
  for(int i = 0; i < 10; i++) {
    printf("Test app 2.\n");
  }

  exit(0);
  // Can just return here. The application will continue to execute.
  // return 0;
}
